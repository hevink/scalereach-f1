import { pageMetadata } from "@/lib/seo";

export const metadata = pageMetadata.twoFactorVerify;

export default function TwoFactorVerifyLayout({
    children,
}: {
    children: React.ReactNode;
}) {
    return children;
}
